import { ReactNode } from 'react';
interface CPBodyProps {
    children: ReactNode;
}
declare const CPBody: ({ children }: CPBodyProps) => import("react/jsx-runtime").JSX.Element;
export default CPBody;
